// Flip to `true` once a live merchant feed (e.g. Awin) is connected and offer
// prices are real. While `false`, prices are simulated/demo and the UI shows
// them as indicative ("≈", "prix indicatifs") so nothing looks authoritative.
export const PRICES_LIVE = false;
